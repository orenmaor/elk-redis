input {
  redis {
    host => "127.0.0.1"
    type => "redis-input"
    threads => 10
    data_type => "list"
    key => "logstash"
    password => "<%= node[:redis][:password] %>"
  }
}

output {
  elasticsearch {
    cluster => "logstash"
    protocol => "http"
  }
}
